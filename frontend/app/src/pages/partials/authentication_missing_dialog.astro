---
import { i18n } from '@helpers/i18n'
const { t } = i18n(Astro.url)

import { marked } from 'marked';
import { renderer } from '@helpers/marked';
---

<p set:html={marked.parseInline(t('authentication_missing_dialog'), { renderer })} />