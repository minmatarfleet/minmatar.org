---
import { i18n } from '@helpers/i18n'
const { t, translatePath } = i18n(Astro.url)

import { HTTP_403_Forbidden } from '@helpers/http_responses'

import type { User } from '@dtypes/jwt'
import * as jose from 'jose'

const auth_token = Astro.cookies.has('auth_token') ? (Astro.cookies.get('auth_token')?.value as string) : false
const user:User | false = auth_token ? jose.decodeJwt(auth_token) as User : false
const is_superuser = (user ? user?.is_superuser : false)

if (!user)
    return HTTP_403_Forbidden()

import { get_user_permissions } from '@helpers/permissions'
const user_permissions = (user ? await get_user_permissions(user?.username) : [])

const can_view_campaigns = is_superuser || user_permissions.includes('campaigns.view_campaign')

if (!can_view_campaigns)
    return HTTP_403_Forbidden()

import type { CampaignOrder } from '@dtypes/api.minmatar.org'
import { get_campaign_orders } from '@helpers/api.minmatar.org/campaigns'

const slug = Astro.url.searchParams.get('slug') as string
const delay = parseInt(Astro.url.searchParams.get('delay') ?? '0')

let fetch_orders_error:Error | false = false
let orders:CampaignOrder[] = []

try {
    orders = await get_campaign_orders(slug, auth_token as string)
} catch (error) {
    fetch_orders_error = error
}

const ORDERS_PARTIAL_URL = translatePath(`/partials/campaign_orders_component?slug=${slug}`)

import CampaignOrdersList from '@components/blocks/CampaignOrdersList.astro'
import ErrorRefetch from '@components/blocks/ErrorRefetch.astro'
---

{fetch_orders_error ?
    <ErrorRefetch
        args={{
            partial: ORDERS_PARTIAL_URL,
            error: fetch_orders_error,
            delay: delay,
        }}
    />
    :
    <CampaignOrdersList orders={orders} />
}
