---
import { i18n } from '@helpers/i18n'
const { translatePath } = i18n(Astro.url)

import { certificatePath } from '@/data/guides/urls'

const slug = Astro.params.slug ?? ''
return Astro.redirect(translatePath(certificatePath(slug)), 301)
---
