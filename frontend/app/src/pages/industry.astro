---
/* Industry hub removed — Market is the home for Orders / Supply Chain */

import { i18n } from '@helpers/i18n'
const { translatePath } = i18n(Astro.url)

return Astro.redirect(`${translatePath('/market/')}${Astro.url.search}`, 301)
---
