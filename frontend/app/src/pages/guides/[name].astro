---
import { i18n } from '@helpers/i18n'
const { translatePath } = i18n(Astro.url)

import { guidePath } from '@/data/guides/urls'

const slug = Astro.params.name ?? ''
return Astro.redirect(translatePath(guidePath(slug)), 301)
---
