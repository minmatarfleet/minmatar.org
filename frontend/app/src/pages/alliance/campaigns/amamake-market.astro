---
import { i18n } from '@helpers/i18n'
const { translatePath } = i18n(Astro.url)

import { CANONICAL_PATH } from '@/data/campaigns/amamake-market'

return Astro.redirect(translatePath(CANONICAL_PATH))
---
