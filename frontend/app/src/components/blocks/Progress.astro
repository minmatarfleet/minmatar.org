<progress {...Astro.props}><slot /></progress>

<style lang="scss">
    progress {
        --progress-height: var(--space-3xs);
        --progress-color: var(--highlight);
        --progress-glow-color: var(--highlight);

        display: block;

        &.danger {
            --progress-color: var(--fleet-red);
            --progress-glow-color: var(--fleet-red);
        }

        &.success {
            --progress-color: var(--green);
            --progress-glow-color: var(--green);
        }

        &.disabled {
            --progress-color: rgba(255, 255, 255, 0.2);
            --progress-glow-color: transparent;
        }

        &[value] {
            -webkit-appearance: none;
            appearance: none;

            -webkit-animation: expand 3s cubic-bezier(0.075, 0.820, 0.165, 1.000) both;
	        animation: expand 3s cubic-bezier(0.075, 0.820, 0.165, 1.000) both;
            height: var(--progress-height);
        }

        &[value]::-webkit-progress-bar {
            background-color: transparent;
            border-radius: 0;
        }

        &[value]::-webkit-progress-value {
            border-radius: 0; 
            background-color: var(--progress-color);
            box-shadow: 0 0 10px 0px var(--progress-glow-color);
        }
    }

    @keyframes expand {
        from {
            width: 0;
        }

        to {
            width: 100%;
        }
    }
</style>