---
import {
    guideMeta,
    introduction,
    considerations,
    considerationBullets,
    disclaimers,
    metagameSummary,
    glossary,
    guideSections,
    additionalResources,
    matchupLegend,
    matchupCharts,
    shipGuides,
} from '@/data/navy-frigate-guide'

import GuideContentsLayout from '@components/blocks/GuideContentsLayout.astro'
import NavyFrigateGuideMatchupChart from '@components/blocks/navy-frigate-guide/NavyFrigateGuideMatchupChart.astro'
import NavyFrigateGuideShipSection from '@components/blocks/navy-frigate-guide/NavyFrigateGuideShipSection.astro'
---

<div class="nfg-guide card component light-transparency border">
    <header class="nfg-guide__masthead">
        <p class="nfg-guide__series">{guideMeta.title}</p>
        <p class="nfg-guide__edition">{guideMeta.edition} · {guideMeta.yc}</p>
    </header>

    <GuideContentsLayout sections={guideSections}>
            <section id="introduction" data-section-id="introduction" class="nfg-section nfg-panel" aria-labelledby="nfg-intro-title">
                <h2 id="nfg-intro-title" class="nfg-section__title">Introduction</h2>
                {
                    introduction.map((paragraph) => (
                        <p class="nfg-section__paragraph" set:html={paragraph} />
                    ))
                }
            </section>

            <section id="considerations" data-section-id="considerations" class="nfg-section nfg-panel" aria-labelledby="nfg-considerations-title">
                <h2 id="nfg-considerations-title" class="nfg-section__title">Considerations of the Metagame</h2>
                {
                    considerations.map((paragraph) => (
                        <p class="nfg-section__paragraph">{paragraph}</p>
                    ))
                }
                <ul class="nfg-section__list">
                    {
                        considerationBullets.map((bullet) => (
                            <li>{bullet}</li>
                        ))
                    }
                </ul>
            </section>

            <section id="disclaimers" data-section-id="disclaimers" class="nfg-section nfg-panel" aria-labelledby="nfg-disclaimers-title">
                <h2 id="nfg-disclaimers-title" class="nfg-section__title">Disclaimers</h2>
                {
                    disclaimers.map((paragraph) => (
                        <p class="nfg-section__paragraph">{paragraph}</p>
                    ))
                }
            </section>

            <section id="summary" data-section-id="summary" class="nfg-section nfg-panel" aria-labelledby="nfg-summary-title">
                <h2 id="nfg-summary-title" class="nfg-section__title">Summary of the Metagame</h2>
                <div class="nfg-summary-grid">
                    {
                        metagameSummary.map((entry) => (
                            <article class="nfg-summary-card">
                                <h3 class="nfg-summary-card__ship">{entry.ship}</h3>
                                <p class="nfg-summary-card__text">{entry.text}</p>
                            </article>
                        ))
                    }
                </div>
            </section>

            <section id="matchups" data-section-id="matchups" class="nfg-section nfg-panel" aria-labelledby="nfg-matchups-title">
                <h2 id="nfg-matchups-title" class="nfg-section__title">Matchup Charts</h2>

                <div class="nfg-legend" aria-label="Matchup rating legend">
                    {
                        matchupLegend.map((item) => (
                            <div class="nfg-legend__item">
                                <span class:list={['nfg-legend__symbol', `nfg-legend__symbol--${item.rating === '?' ? 'unknown' : item.rating.replace(/\+/g, 'plus').replace(/-/g, 'minus').replace(/=/g, 'even')}`]}>
                                    {item.rating}
                                </span>
                                <span class="nfg-legend__label">
                                    <strong>{item.label}</strong>
                                    <span>{item.description}</span>
                                </span>
                            </div>
                        ))
                    }
                </div>

                <div class="nfg-charts-grid">
                    {
                        matchupCharts.map((chart) => (
                            <NavyFrigateGuideMatchupChart chart={chart} />
                        ))
                    }
                </div>
            </section>

            <div class="nfg-guide__ships">
                {
                    shipGuides.map((ship) => (
                        <NavyFrigateGuideShipSection ship={ship} />
                    ))
                }
            </div>

            <section id="glossary" data-section-id="glossary" class="nfg-section nfg-panel" aria-labelledby="nfg-glossary-title">
                <h2 id="nfg-glossary-title" class="nfg-section__title">Glossary</h2>
                <dl class="nfg-glossary">
                    {
                        glossary.map((entry) => (
                            <>
                                <dt class="nfg-glossary__term">{entry.term}</dt>
                                <dd class="nfg-glossary__definition">{entry.definition}</dd>
                            </>
                        ))
                    }
                </dl>
            </section>

            <section id="resources" data-section-id="resources" class="nfg-section nfg-panel" aria-labelledby="nfg-resources-title">
                <h2 id="nfg-resources-title" class="nfg-section__title">Additional Resources</h2>
                <p class="nfg-section__paragraph">
                    This guide stands on its own. The links below are where we looked for community practice, plex fundamentals, and deeper yearly data—use them when you want another angle.
                </p>
                <ul class="nfg-resources">
                    {
                        additionalResources.map((resource) => (
                            <li class="nfg-resources__item">
                                <a
                                    href={resource.href}
                                    target={resource.href.startsWith('http') ? '_blank' : undefined}
                                    rel={resource.href.startsWith('http') ? 'nofollow noopener noreferrer' : undefined}
                                    class="nfg-guide__link"
                                >
                                    {resource.title}
                                </a>
                                <span class="nfg-resources__note">{resource.note}</span>
                            </li>
                        ))
                    }
                </ul>
            </section>
    </GuideContentsLayout>
</div>

<style lang="scss">
    @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@500;600;700&family=Source+Sans+3:ital,wght@0,400;0,600;1,400&display=swap');

    .nfg-guide {
        --nfg-display-font: 'Orbitron', var(--heading-font);
        --nfg-body-font: 'Source Sans 3', var(--body-font);
        --nfg-panel-padding-block: var(--component-padding-block);
        --nfg-panel-padding-inline: var(--space-l-xl);
        width: 100%;
        max-width: 100%;
        min-width: 0;
        overflow-x: clip;
        padding: var(--space-2xl) var(--space-l-xl);
        font-family: var(--nfg-body-font);
    }

    @media (max-width: 48rem) {
        .nfg-guide {
            padding: var(--space-l) var(--space-s);
        }
    }

    .nfg-panel {
        padding: var(--nfg-panel-padding-block) var(--nfg-panel-padding-inline);
        background: color-mix(in srgb, var(--component-background) 72%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 70%, transparent);
        min-width: 0;
    }

    @media (max-width: 48rem) {
        .nfg-panel {
            --nfg-panel-padding-inline: var(--space-s);
        }
    }

    .nfg-guide__masthead {
        margin-bottom: var(--space-2xl);
        padding-bottom: var(--space-xl);
        text-align: center;
        border-bottom: 1px solid color-mix(in srgb, var(--border-color) 65%, transparent);
    }

    .nfg-guide__series {
        margin: 0;
        font-family: var(--nfg-display-font);
        font-size: clamp(var(--step-1), 3vw, var(--step-3));
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--highlight);
    }

    .nfg-guide__edition {
        margin: var(--space-s) 0 0;
        font-size: var(--step--1);
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: var(--text-color-secondary, var(--faded));
    }

    .nfg-guide__ships {
        display: flex;
        flex-direction: column;
        gap: var(--space-l);
    }

    .nfg-section {
        scroll-margin-top: calc(var(--header-height, 4rem) + var(--space-l));
    }

    .nfg-section__title {
        margin: 0 0 var(--space-m);
        padding-bottom: var(--space-s);
        border-bottom: 1px solid color-mix(in srgb, var(--border-color) 55%, transparent);
        font-family: var(--nfg-display-font);
        font-size: clamp(var(--step-1), 2.5vw, var(--step-2));
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .nfg-section__subtitle {
        margin: var(--space-l) 0 var(--space-m);
        font-family: var(--nfg-display-font);
        font-size: var(--step-0);
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--highlight);
    }

    .nfg-section__paragraph {
        margin: 0 0 var(--space-m);
        font-size: var(--step-0);
        line-height: 1.7;
        max-width: var(--max-text-width);
    }

    .nfg-section__paragraph :global(a) {
        color: var(--highlight);
        text-decoration: underline;
        text-underline-offset: 0.15em;
    }

    .nfg-section__list {
        margin: 0;
        padding-left: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: var(--space-s);
        line-height: 1.65;
        max-width: var(--max-text-width);
    }

    .nfg-section__cover {
        margin: 0 0 var(--space-xl);
    }

    .nfg-section__cover-image,
    .nfg-section__figure-image {
        display: block;
        width: 100%;
        border: 1px solid color-mix(in srgb, var(--border-color) 70%, transparent);
    }

    .nfg-section__figure {
        margin: var(--space-xl) 0 0;
    }

    .nfg-section__figcaption {
        margin-top: var(--space-xs);
        font-size: var(--step--2);
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--text-color-secondary, var(--faded));
    }

    .nfg-guide__link {
        color: var(--highlight);
        text-decoration: underline;
        text-decoration-color: color-mix(in srgb, var(--fleet-yellow) 45%, transparent);
        text-underline-offset: 0.15em;

        &:hover,
        &:focus-visible {
            color: var(--fleet-yellow);
            text-decoration-color: currentColor;
        }
    }

    .nfg-summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(min(100%, 16rem), 1fr));
        gap: var(--space-m);
        margin-top: var(--space-m);
    }

    .nfg-summary-card {
        padding: var(--component-padding-block) var(--space-m);
        background: color-mix(in srgb, var(--component-background) 82%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 70%, transparent);
    }

    .nfg-summary-card__ship {
        margin: 0 0 var(--space-s);
        font-family: var(--nfg-display-font);
        font-size: var(--step--1);
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--fleet-red);
    }

    .nfg-summary-card__text {
        margin: 0;
        font-size: var(--step--1);
        line-height: 1.6;
    }

    .nfg-legend {
        display: flex;
        flex-wrap: wrap;
        gap: var(--space-xs);
        margin-bottom: var(--space-l);
        padding: var(--space-s);
        background: color-mix(in srgb, var(--component-background) 55%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 55%, transparent);
    }

    .nfg-legend__item {
        display: flex;
        align-items: center;
        gap: var(--space-xs);
        padding: var(--space-xs) var(--space-s);
        background: color-mix(in srgb, var(--component-background) 78%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 60%, transparent);
    }

    .nfg-legend__symbol {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 1.75rem;
        min-height: 1.75rem;
        border-radius: 999px;
        font-family: var(--nfg-display-font);
        font-size: var(--step--1);
        font-weight: 700;
    }

    .nfg-legend__symbol--plusplus { background: color-mix(in srgb, #2ecc71 22%, transparent); color: #7dffb0; }
    .nfg-legend__symbol--plus { background: color-mix(in srgb, #58d68d 18%, transparent); color: #9ef0b8; }
    .nfg-legend__symbol--even { background: color-mix(in srgb, #5dade2 18%, transparent); color: #a8d8ff; }
    .nfg-legend__symbol--minus { background: color-mix(in srgb, #e74c3c 16%, transparent); color: #ff9f96; }
    .nfg-legend__symbol--minusminus { background: color-mix(in srgb, #c0392b 20%, transparent); color: #ff8a80; }
    .nfg-legend__symbol--unknown { background: color-mix(in srgb, var(--faded) 18%, transparent); color: var(--text-color-secondary, var(--faded)); }

    .nfg-legend__label {
        display: flex;
        flex-direction: column;
        gap: 0.1rem;
        font-size: var(--step--1);

        strong {
            font-size: var(--step--1);
        }

        span {
            font-size: var(--step--2);
            color: var(--text-color-secondary, var(--faded));
        }
    }

    @media (min-width: 48rem) {
        .nfg-legend__label {
            flex-direction: row;
            align-items: baseline;
            gap: var(--space-2xs);
        }

        .nfg-legend__label span {
            font-size: var(--step--1);
        }
    }

    .nfg-charts-grid {
        display: flex;
        flex-direction: column;
        gap: var(--space-2xl);
    }

    .nfg-glossary {
        display: grid;
        grid-template-columns: minmax(6rem, 10rem) 1fr;
        gap: var(--space-s) var(--space-l);
        margin: 0 0 var(--space-xl);
    }

    .nfg-glossary__term {
        margin: 0;
        font-weight: 700;
        color: var(--fleet-yellow);
    }

    .nfg-glossary__definition {
        margin: 0;
        line-height: 1.6;
    }

    .nfg-resources {
        margin: 0;
        padding: 0;
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: var(--space-m);
        max-width: var(--max-text-width);
    }

    .nfg-resources__item {
        display: flex;
        flex-direction: column;
        gap: var(--space-3xs);
        line-height: 1.55;
    }

    .nfg-resources__note {
        font-size: var(--step--1);
        color: var(--text-color-secondary, var(--faded));
    }

    @media (max-width: 40rem) {
        .nfg-glossary {
            grid-template-columns: 1fr;
        }
    }
</style>
