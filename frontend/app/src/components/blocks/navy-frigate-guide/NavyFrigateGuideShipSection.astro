---
import NavyFrigateGuideFitCard from '@components/blocks/navy-frigate-guide/NavyFrigateGuideFitCard.astro'
import NavyFrigateGuideMatchupCard from '@components/blocks/navy-frigate-guide/NavyFrigateGuideMatchupCard.astro'
import ItemPicture from '@components/blocks/ItemPicture.astro'
import { getGuideFittingsForShip } from '@/data/navy-frigate-guide'
import type { MatchupVerdict, ShipGuide } from '@/data/navy-frigate-guide'
import { resolveGuideFittings } from '@helpers/guide_fittings'
import type { Fitting } from '@dtypes/api.minmatar.org'

interface Props {
    ship: ShipGuide
    fittingLibrary?: Fitting[]
}

const { ship, fittingLibrary = [] } = Astro.props
const fits = resolveGuideFittings(getGuideFittingsForShip(ship.id), fittingLibrary)

const verdictLabels: Record<MatchupVerdict, string> = {
    favoured: 'Favoured',
    unfavoured: 'Unfavoured',
    even: 'Coinflip',
    bail: 'Disengage',
    skill: 'Skill check',
    unknown: '?',
}
---

<section id={ship.id} data-section-id={ship.id} class="nfg-ship" aria-labelledby={`${ship.id}-title`}>
    <div class="nfg-ship__hero">
        <div class="nfg-ship__hero-visual">
            <ItemPicture
                item_id={ship.shipId}
                item_name={ship.name}
                size={512}
                narrow={true}
                icon_quality={512}
                mask={true}
            />
        </div>
        <div class="nfg-ship__stats">
            <p class="nfg-ship__stats-kicker">{ship.faction} Frigate · {ship.shortName}</p>
            <h3 id={`${ship.id}-title`} class="nfg-ship__stats-title">{ship.name}</h3>
            <p class="nfg-ship__tagline">{ship.tagline}</p>
            <dl class="nfg-ship__bonuses">
                <dt class="nfg-ship__bonuses-label">Ship bonuses (per skill level)</dt>
                {
                    ship.bonuses.map((bonus) => (
                        <div class="nfg-ship__bonus-row">
                            <span class="nfg-ship__bonus-value">{bonus.value}</span>
                            <span class="nfg-ship__bonus-text">{bonus.label}</span>
                        </div>
                    ))
                }
                <dt class="nfg-ship__bonuses-label">Role bonus</dt>
                <dd class="nfg-ship__role-bonus">{ship.roleBonus}</dd>
            </dl>
        </div>
    </div>

    <div class="nfg-ship__body">
        {
            ship.sections.map((section) => (
                <section class="nfg-ship__topic" aria-labelledby={`${ship.id}-${section.id}`}>
                    <h4 id={`${ship.id}-${section.id}`} class="nfg-ship__topic-title">{section.title}</h4>
                    {
                        section.paragraphs.map((paragraph) => (
                            <p class="nfg-ship__paragraph">{paragraph}</p>
                        ))
                    }
                    {
                        section.bullets && section.bullets.length > 0 && (
                            <ul class="nfg-ship__bullets">
                                {section.bullets.map((bullet) => (
                                    <li>{bullet}</li>
                                ))}
                            </ul>
                        )
                    }
                </section>
            ))
        }

        <section class="nfg-ship__fits" aria-labelledby={`${ship.id}-fits-title`}>
            <h4 id={`${ship.id}-fits-title`} class="nfg-ship__fits-title">Example fits</h4>
            <div class="nfg-ship__fits-grid">
                {
                    fits.map((fit) => (
                        <NavyFrigateGuideFitCard fit={fit} shipName={ship.name} />
                    ))
                }
            </div>
        </section>

        {
            ship.matchups.map((group) => {
                const groupSlug = group.title.replace(/\s+/g, '-').toLowerCase()
                return (
                    <section class="nfg-ship__matchups" aria-labelledby={`${ship.id}-matchup-${groupSlug}`}>
                        <h4 id={`${ship.id}-matchup-${groupSlug}`} class="nfg-ship__section-title">
                            Matchups · {group.title}
                        </h4>
                        {group.fitContext && <p class="nfg-ship__matchup-context">{group.fitContext}</p>}
                        <ul class="nfg-ship__matchup-cards">
                            {
                                group.entries.map((entry) => (
                                    <NavyFrigateGuideMatchupCard
                                        entry={entry}
                                        verdictLabels={verdictLabels}
                                        selfShipId={ship.shipId}
                                        selfShipName={ship.name}
                                    />
                                ))
                            }
                        </ul>
                    </section>
                )
            })
        }
    </div>
</section>

<style lang="scss">
    .nfg-ship {
        scroll-margin-top: calc(var(--header-height, 4rem) + var(--space-l));
        min-width: 0;
        overflow-x: clip;
        padding: var(--component-padding-block) var(--space-l-xl);
        background: color-mix(in srgb, var(--component-background) 72%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 70%, transparent);
    }

    @media (max-width: 48rem) {
        .nfg-ship {
            padding-inline: var(--space-s);
        }
    }

    .nfg-ship__hero {
        display: grid;
        gap: var(--space-l);
        margin-bottom: var(--space-l);
        padding-bottom: var(--space-l);
        border-bottom: 1px solid color-mix(in srgb, var(--border-color) 55%, transparent);
        align-items: stretch;
    }

    @media (min-width: 48rem) {
        .nfg-ship__hero {
            grid-template-columns: minmax(0, 1fr) minmax(16rem, 22rem);
            align-items: end;
        }
    }

    .nfg-ship__hero-visual {
        display: flex;
        justify-content: center;
        overflow: hidden;
        padding: var(--component-padding-block);
        background:
            radial-gradient(circle at 50% 40%, color-mix(in srgb, var(--fleet-red) 12%, transparent), transparent 70%),
            color-mix(in srgb, var(--component-background) 80%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 70%, transparent);

        :global(picture) {
            width: min(100%, 512px);
            max-width: 100%;
        }

        :global(img) {
            width: 100%;
            height: auto;
        }
    }

    .nfg-ship__stats {
        min-width: 0;
        padding: var(--component-padding-block) var(--space-m);
        background: color-mix(in srgb, var(--component-background) 92%, transparent);
        border: 1px solid color-mix(in srgb, var(--border-color) 80%, transparent);
    }

    .nfg-ship__stats-kicker {
        margin: 0 0 var(--space-2xs);
        font-size: var(--step--2);
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: var(--fleet-red);
    }

    .nfg-ship__stats-title {
        margin: 0 0 var(--space-2xs);
        font-family: var(--heading-font);
        font-size: var(--step-0);
        letter-spacing: 0.06em;
        text-transform: uppercase;
    }

    .nfg-ship__tagline {
        margin: 0 0 var(--space-m);
        font-size: var(--step--1);
        line-height: 1.5;
        color: var(--text-color-secondary, var(--faded));
        font-style: italic;
    }

    .nfg-ship__bonuses {
        margin: 0;
    }

    .nfg-ship__bonuses-label {
        margin: var(--space-s) 0 var(--space-2xs);
        font-size: var(--step--2);
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--text-color-secondary, var(--faded));
    }

    .nfg-ship__bonus-row {
        display: flex;
        gap: var(--space-xs);
        align-items: baseline;
        margin-bottom: var(--space-3xs);
    }

    .nfg-ship__bonus-value,
    .nfg-ship__bonus-text,
    .nfg-ship__role-bonus {
        margin: 0;
        font-size: var(--step--1);
        line-height: 1.35;
    }

    .nfg-ship__bonus-value {
        flex-shrink: 0;
        font-weight: 700;
        color: var(--highlight);
        white-space: nowrap;
    }

    .nfg-ship__bonus-text {
        min-width: 0;
    }

    .nfg-ship__body {
        display: flex;
        flex-direction: column;
        gap: var(--space-l);
    }

    .nfg-ship__topic {
        display: flex;
        flex-direction: column;
        gap: var(--space-s);
        padding-bottom: var(--space-m);
        border-bottom: 1px solid color-mix(in srgb, var(--border-color) 45%, transparent);
    }

    .nfg-ship__topic-title {
        margin: 0;
        font-family: var(--heading-font);
        font-size: var(--step--1);
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--fleet-yellow);
    }

    .nfg-ship__paragraph {
        margin: 0;
        font-size: var(--step-0);
        line-height: 1.65;
        max-width: var(--max-text-width);
    }

    .nfg-ship__bullets {
        margin: 0;
        padding-left: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: var(--space-xs);
        font-size: var(--step--1);
        line-height: 1.6;
    }

    .nfg-ship__fits,
    .nfg-ship__matchups {
        padding-top: var(--space-s);
    }

    .nfg-ship__fits-title,
    .nfg-ship__section-title {
        margin: 0 0 var(--space-s);
        padding-bottom: var(--space-2xs);
        border-bottom: 1px solid color-mix(in srgb, var(--border-color) 50%, transparent);
        font-family: var(--heading-font);
        letter-spacing: 0.06em;
        text-transform: uppercase;
    }

    .nfg-ship__section-title {
        font-size: var(--step--1);
        color: var(--highlight);
    }

    .nfg-ship__fits-title {
        font-size: var(--step-0);
        color: var(--fleet-yellow);
    }

    .nfg-ship__matchup-context {
        margin: 0 0 var(--space-s);
        font-size: var(--step--1);
        line-height: 1.5;
        color: var(--text-color-secondary, var(--faded));
    }

    .nfg-ship__fits-grid {
        display: flex;
        flex-direction: column;
        gap: var(--space-3xs);
    }

    .nfg-ship__matchups {
        display: flex;
        flex-direction: column;
        gap: var(--space-s);
    }

    .nfg-ship__matchup-cards {
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--space-3xs);
        align-items: stretch;
        margin: 0;
        padding: 0;
        list-style: none;
    }

    @media (min-width: 36rem) {
        .nfg-ship__matchup-cards {
            grid-template-columns: repeat(auto-fill, minmax(min(100%, 18rem), 1fr));
        }
    }
</style>
