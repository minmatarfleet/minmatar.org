<kbd><slot /></kbd>

<style lang="scss">
    kbd {
        background-color: var(--component-background);
        border-radius: 3px;
        border: 1px solid var(--border-color);
        box-shadow: 0 1px 1px rgba(0, 0, 0, 0.2), 0 2px 0 0 rgba(25, 25, 25, 0.7) inset;
        color: var(--highlight);
        display: inline-block;
        font-size: 0.85em;
        font-weight: 700;
        line-height: 1;
        padding: 2px 4px;
        white-space: nowrap;
    }
</style>