---
interface Props {
    character_id:   number;
    character_name: string;
    inert?:         boolean;
    size?:          'sm' | 'lg';
    [propName: string]: any;
}

const {
    character_id,
    character_name,
    inert = false,
    size =  'lg',
    ...attributes
} = Astro.props

import CharacterPicture from "@components/blocks/CharacterPicture.astro";
import Card from "@components/blocks/Card.astro";
---

<Card {...attributes}>
    <CharacterPicture
        slot="cover"
        character_id={character_id}
        character_name={character_name}
        size={256}
        narrow={true}
        icon_quality={512}
        mask={true}
        inert={inert}
        height={size === 'sm' ? 320 : undefined}
    />

    <slot name="header" slot="header" />
    <slot name="actions" slot="actions" />
    <slot name="visible-actions" slot="visible-actions" />
    <slot />
</Card>