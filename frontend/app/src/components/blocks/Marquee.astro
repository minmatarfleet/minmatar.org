---
import FlexInline from '@components/compositions/FlexInline.astro';
---

<div 
    class="marquee-container"
    x-data="{ 
        speed: 30, // Pixels per second
        init() {
            $dispatch('marquee-started')
            
            $nextTick(() => {
                const content = $refs.content;
                // We only measure half because we duplicated the items
                const totalWidth = content.scrollWidth / 2; 
                const duration = totalWidth / this.speed;
                content.style.setProperty('--duration', `${duration}s`);
            })
        }
    }"
>
    <FlexInline 
        class="marquee-content" 
        x-ref="content"
        gap='var(--space-l)'
    >
        <slot />
    </FlexInline>
</div>

<style>
    .marquee-container {
        overflow: hidden;
        width: 100%;
        mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
    }

    .marquee-content {
        display: flex;
        width: max-content;
        animation: scroll var(--duration, 0s) linear infinite;
    }

    @keyframes scroll {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
    }
</style>