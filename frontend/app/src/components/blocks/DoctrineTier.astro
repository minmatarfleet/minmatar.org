---
import { i18n } from '@helpers/i18n'
const { t } = i18n(Astro.url)

import type { FittingItem } from '@dtypes/layout_components';

interface Props {
    fittings:           FittingItem[];
}

const {
    fittings,
} = Astro.props

import Grid from '@components/compositions/Grid.astro';
import FittingCard from '@components/blocks/FittingCard.astro';

import FittingLink from '@components/blocks/FittingLink.astro'
import { format_fitting_tags_line } from '@helpers/fitting_tags'
---

<Grid
    role="list"
    class="[ w-full grid-fill ]"
    min_item_width='240px'
    row_gap='var(--space-3xs)'
    column_gap='var(--space-3xs)'
>
    {fittings.map((fitting) =>
        <FittingLink
            fitting_id={fitting.id}
            class="[ items-center flex ]"
            role="listitem"
        >
            <FittingCard
                fitting={fitting}
                title={fitting.ship_name}
                subtitle={format_fitting_tags_line(fitting.tags, t) || fitting.fitting_type}
            />
        </FittingLink>
    )}
</Grid>