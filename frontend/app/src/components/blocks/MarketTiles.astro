---
import { i18n } from '@helpers/i18n'
const { t, translatePath } = i18n(Astro.url)

import Wrapper from '@components/compositions/Wrapper.astro';
import Grid from '@components/compositions/Grid.astro';

import Button from '@components/blocks/Button.astro';
import Tile from '@components/blocks/Tile.astro';

import MarketDeliveriesEVEIcon from '@components/icons/MarketDeliveriesEVEIcon.astro';
import FreightServiceEvEIcon from '@components/icons/FreightServiceEvEIcon.astro';
---

<Wrapper
    class="[ market-reel ]"
    padding_block='0'
    padding_inline='0'
    max_width='100%'
>
    <Grid
        class="[ w-full grid-fill ]"
        row_gap="var(--space-3xs)"
        column_gap="var(--space-3xs)"
        min_item_width="580px"
    >
        <Tile
            title={t('industry.orders.page_title')}
            description={t('industry.orders.leading_text')}
            cover='/images/orders-tile-background.webp'
        >
            <Button size='sm' href={translatePath('/industry/orders/')}>
                {t('browse')}
            </Button>

            <Button size='sm' href={translatePath('/industry/blueprints/')}>
                {t('find_blueprints')}
            </Button>
        </Tile>

        <Tile
            title={t('freight.page_title')}
            description={t('freight.leading_text')}
            cover='/images/freight-tile-background.webp'
        >
            <FreightServiceEvEIcon class="[ ml-[-7px] ]" slot="icon" />

            <Button size='sm' href={translatePath('/market/freight/calculator/')}>
                {t('freight.calculator.page_title')}
            </Button>
            <Button size='sm' href={translatePath('/market/freight/contracts/')}>
                {t('freight_contracts.page_title')}
            </Button>
        </Tile>

        <Tile
            title={t('staging_supply.ops.tile_title')}
            description={t('staging_supply.ops.tile_description')}
            cover='/images/sellorders-tile-background.webp'
        >
            <MarketDeliveriesEVEIcon class="[ ml-[-7px] ]" slot="icon" />

            <Button size='sm' href={translatePath('/market/ops/')}>
                {t('staging_supply.ops.tile_browse')}
            </Button>
        </Tile>

        <Tile
            title={t('industry.products.page_title')}
            description={t('industry.products.leading_text')}
            cover='/images/products-tile-background.webp'
        >
            <Button size='sm' href={translatePath('/industry/products/')}>
                {t('browse')}
            </Button>
        </Tile>
    </Grid>
</Wrapper>
