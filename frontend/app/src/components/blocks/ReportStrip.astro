---
import type { CampaignMeta } from '@/data/campaigns'

import TextBox from '@components/layout/TextBox.astro'
import Flexblock from '@components/compositions/Flexblock.astro'
import Wrapper from '@components/compositions/Wrapper.astro'

import LandingSwiper from '@components/blocks/LandingSwiper.astro'
import CampaignPostCard from '@components/blocks/CampaignPostCard.astro'

interface Props {
    /** Report cards to show, newest first. Renders nothing when empty. */
    reports: CampaignMeta[]
    title: string
    leading_text: string
    /** Heading id used as the in-page anchor (e.g. `#WarzoneReports`). */
    anchor_id: string
    swiper_id: string
    show_isk?: boolean
}

const { reports, title, leading_text, anchor_id, swiper_id, show_isk = false } = Astro.props
---

{reports.length > 0 &&
    <section class="[ content-section relative ]">
        <Flexblock gap='var(--space-l)'>
            <TextBox>
                <Flexblock>
                    <h2 id={anchor_id}>{title}</h2>
                    <p>{leading_text}</p>
                </Flexblock>
            </TextBox>

            <Wrapper
                padding_block='0'
                padding_inline='0'
                max_width='100%'
            >
                <Wrapper
                    class="[ report-reel ]"
                    padding_block='0'
                    padding_inline='0'
                    max_width='100%'
                >
                    <div id={swiper_id} class="[ swiper ][ w-full pb-10! ]">
                        <LandingSwiper swiper_selector={`#${swiper_id}`} align='end' gap={10}>
                            {reports.map((report) => (
                                <div
                                    class="[ swiper-slide ]"
                                    style="margin-right: 10px"
                                >
                                    <CampaignPostCard campaign={report} show_isk={show_isk} />
                                </div>
                            ))}
                        </LandingSwiper>
                    </div>

                    <div id={`${swiper_id}-button-prev`} class="[ swiper-button-prev hidden lg:block! ]"></div>
                    <div id={`${swiper_id}-button-next`} class="[ swiper-button-next hidden lg:block! ]"></div>
                </Wrapper>
            </Wrapper>
        </Flexblock>
    </section>
}

<style lang="scss">
    h2 {
        font-size: var(--step-3);
    }

    .report-reel {
        position: relative;
    }

    .swiper-button-prev,
    .swiper-button-next {
        --swiper-navigation-color: var(--highlight);
        --swiper-navigation-sides-offset: -46px;

        opacity: 0;
        z-index: 1;
    }

    .swiper-initialized ~ .swiper-button-prev,
    .swiper-initialized ~ .swiper-button-next {
        opacity: 1;
    }

    .swiper-initialized ~ .swiper-button-next.swiper-button-disabled,
    .swiper-initialized ~ .swiper-button-prev.swiper-button-disabled {
        opacity: 0;
    }

    .swiper-button-next.swiper-button-disabled,
    .swiper-button-prev.swiper-button-disabled {
        --swiper-navigation-color: var(--fleet-yellow);
        opacity: 1;
    }

    .swiper-button-prev,
    .swiper-button-next {
        transform: translateY(-1.25rem);
    }

    .swiper-slide {
        width: min(100%, 320px);
    }
</style>
