---
import { i18n } from '@helpers/i18n'
const { t } = i18n(Astro.url)

interface Props {
    is_random:  boolean;
}

const {
    is_random,
} = Astro.props

import Dropdown from '@components/blocks/Dropdown.astro';
import StationInfo from '@components/blocks/StationInfo.astro';
import StationsSelector from '@components/blocks/StationsSelector.astro';
---

<Dropdown>
    <StationInfo
        system="Sosala"
        security="0.4"
        constellation="Vaarma"
        region="The Bleak Lands"
        station_name="Sosala – WATERMELON"
        station_description={t('former_hq_description')}
        slot="button"
    />

    <StationsSelector selected={!is_random ? 'watermellon' : undefined} />
</Dropdown>