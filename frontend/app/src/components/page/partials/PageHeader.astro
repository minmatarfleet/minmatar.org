---
interface Props {
    max_width?:         string;
    sticky:             boolean;
    neocom?:            boolean;
    [propName: string]: any;
}

const {
    max_width = '80rem',
    neocom = true,
    sticky,
} = Astro.props;

import Wrapper from '@components/compositions/Wrapper.astro';
import XFadeScroll from '@components/compositions/XFadeScroll.astro';
import FlexInline from '@components/compositions/FlexInline.astro';
import Flexblock from '@components/compositions/Flexblock.astro';
---

<div class:list={[ 'post-header-wrapper ] relative lg:top-0', { 'xl:sticky!': sticky } ]}>
    <Flexblock class="[ post-header ]" gap='0'>
        <div class="[ relative z-10 ]">
            <Wrapper class="[ main-bar ][ w-full ]" padding_block="var(--space-xs)" max_width={max_width} neocom={neocom} centered={true}>
                <slot />
            </Wrapper>
        </div>

        <div class="[ subheader-bar ]">
            <Wrapper class="[ w-full ]" centered={true} max_width={max_width} neocom={neocom} padding_block='0'>
                <XFadeScroll flex>
                    <div class="[ scroll-content ][ w-full ]">
                        <FlexInline class="[ w-max xl:w-full items-start! ]">
                            <div id="mobile-actions" class="[ xl:hidden! ]" />
                            <slot name="subheader" />
                        </FlexInline>
                    </div>
                </XFadeScroll>
            </Wrapper>
        </div>
    </Flexblock>
</div>

<style lang="scss" is:global>
    .post-header > .wrapper > .flex-inline {
        width: 100%;
    }

    .post-header h1 {
        transition: var(--fast-transition);
        font-size: var(--step-4) !important;
    }

    .post-header {
        background-color: var(--dark-transparency-background-mobile);
        padding-top: 0;

        @media screen and (min-width: 980px) {
            background-color: var(--dark-transparency-background);
            backdrop-filter: blur(var(--transparency-backdrop-blur));
        }
    }

    .post-header-wrapper {
        z-index: var(--header-z-index);
    }
    
    @supports (container-type: scroll-state) {
        .post-header-wrapper {
            container-type: scroll-state;
            container-name: sticky-heading;
        }

        .post-header {
            background-color: transparent;
            padding-top: var(--space-xl);
            backdrop-filter: none;
        }

        @container sticky-heading scroll-state(stuck: top) {
            .post-header {
                background-color: var(--dark-transparency-background-mobile);
                padding-top: 0;

                @media screen and (min-width: 980px) {
                    background-color: var(--dark-transparency-background);
                    backdrop-filter: blur(var(--transparency-backdrop-blur));
                }

                h1 {
                    transition: var(--fast-transition);
                    font-size: var(--step-1) !important;
                }
            }
        }
    }

    @media screen and (max-width: 1279px) {
        .subheader-bar:not(:has(a, input, button)) {
            padding-top: 0 !important;
            padding-bottom: 0 !important;
        }
    }
</style>

<style lang="scss">

    .subheader-bar {
        width: 100%;

        @media screen and (max-width: 1279px) {
            position: fixed;
            bottom: 0;
            background-color: var(--dark-transparency-background);
            backdrop-filter: blur(var(--transparency-backdrop-blur));
            padding-top: var(--space-xs);
            padding-bottom: var(--space-xs);

            :global(.subheader-content) {
                width: max-content;
            }
        }

        @media screen and (max-width: 979px) {
            background-color: var(--dark-transparency-background-mobile);
        }
    }
    
    .post-header {
        transition: var(--fast-transition);

        .main-bar {
            position: relative;
            z-index: 1;
            min-height: var(--space-xl-2xl);
            display: flex;
            width: 100%;
            justify-content: stretch;
        }
    }
</style>