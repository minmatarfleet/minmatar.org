# Toolchain versions for this repo. Read by asdf and mise.
#   asdf install     (needs the nodejs/python plugins added once)
#   mise install
#
# nodejs: 22 LTS. Newer majors (24/26) have no prebuilt binary for
# better-sqlite3 and fail to compile it during `npm i` in frontend/app.
# python: matches `python_version` in backend/Pipfile.
nodejs 22.23.3
python 3.10.12
